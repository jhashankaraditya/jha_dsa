class Solution {
public:
    bool areSimilar(vector<vector<int>>& mat, int k) {
        bool same=true;
        for (int i=0; i<mat.size(); i++) {
            for (int j=0; j<mat[i].size(); j++) {
                if (mat[i][(j+k)%mat[i].size()]!=mat[i][j]) {
                    same=false;
                    break;
                }
            }
        }return same;
    }
};

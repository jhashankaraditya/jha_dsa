class Solution {
public:
    int minimumPrefixLength(vector<int>& nums) {
        int n=nums.size(), idx=n-1;
        while (idx>0) {
            if (nums[idx]<=nums[idx-1]) {
                return idx;
            }
            idx--;
        }
        return 0;
    }
};

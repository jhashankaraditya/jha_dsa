class Solution {
public:
    int addDigits(int num) {
        while (num>=10) {
            string number=to_string(num);
            int sum=0;
            for (int i=0; i<number.size(); i++)
                sum+=number[i]-'0';
            num=sum;
        }return num;
    }
};

class Solution {
public:
    bool checkGoodInteger(int n) {
        int sqSum=0, digitSum=0, num=n;
        while (num>0) {
            int digit=(num%10);
            num/=10;
            sqSum+=(digit*digit);
            digitSum+=digit;
        }
        return (sqSum-digitSum>=50);
    }
};

class Solution {
public:
    int minMoves(vector<int>& nums) {
        int operations=0;
        sort(nums.begin(),nums.end());
        for (int i=nums.size()-1; i>=0; i--) {
            operations+=(nums[nums.size()-1]-nums[i]);
        }
        return operations;
    }
};

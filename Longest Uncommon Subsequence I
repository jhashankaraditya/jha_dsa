class Solution {
public:
    int findLUSlength(string a, string b) {
        if (a==b) {
            return -1;
        }
        int aSize=a.size(), bSize=b.size();
        int maxLen=max(aSize,bSize);
        return maxLen;
    }
};
